
  # Build React Web App

  This is a code bundle for Build React Web App. The original project is available at https://www.figma.com/design/bYhAT5eev5ElVmaXTsusJC/Build-React-Web-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  